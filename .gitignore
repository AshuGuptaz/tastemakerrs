node_modules
.next
out
.env
.env.local
.env.development.local
.env.production.local
.DS_Store
*.log
.vercel
.turbo
coverage

# Local MCP server config (contains/relies on local API keys)
.mcp.json

# Agent tooling (installed skills, local agent config) — dev-only, not app code
.agents/
.claude/
skills-lock.json
